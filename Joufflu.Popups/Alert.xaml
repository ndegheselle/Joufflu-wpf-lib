<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:fonts="clr-namespace:Joufflu.Shared.Resources.Fonts;assembly=Joufflu.Shared"
    xmlns:joufflu="clr-namespace:Joufflu;assembly=Joufflu"
    xmlns:local="clr-namespace:Joufflu.Popups">
    <Style x:Key="{x:Type local:Alert}" TargetType="{x:Type local:Alert}">
        <Setter Property="Margin" Value="2" />
        <Setter Property="Width" Value="500" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Alert}">
                    <Border
                        x:Name="AlertContainer"
                        Background="{DynamicResource {x:Static joufflu:Brushes.Background100Brush}}"
                        BorderBrush="{DynamicResource {x:Static joufflu:Brushes.BorderBrush}}"
                        BorderThickness="{DynamicResource {x:Static joufflu:Dimensions.BorderThickness}}"
                        CornerRadius="{DynamicResource {x:Static joufflu:Dimensions.CornerRadius}}">
                        <Border.RenderTransform>
                            <TranslateTransform Y="-200" />
                        </Border.RenderTransform>
                        <Grid>
                            <ProgressBar
                                x:Name="ProgressBarTimer"
                                Height="4"
                                VerticalAlignment="Top"
                                BorderThickness="0">
                                <ProgressBar.Style>
                                    <Style TargetType="ProgressBar">
                                        <Setter Property="Foreground" Value="{DynamicResource {x:Static joufflu:Brushes.InfoBrush}}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Options.Type, RelativeSource={RelativeSource TemplatedParent}}" Value="Warning">
                                                <Setter Property="Foreground" Value="{DynamicResource {x:Static joufflu:Brushes.WarningBrush}}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Options.Type, RelativeSource={RelativeSource TemplatedParent}}" Value="Error">
                                                <Setter Property="Foreground" Value="{DynamicResource {x:Static joufflu:Brushes.DangerBrush}}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Options.Type, RelativeSource={RelativeSource TemplatedParent}}" Value="Success">
                                                <Setter Property="Foreground" Value="{DynamicResource {x:Static joufflu:Brushes.SuccessBrush}}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ProgressBar.Style>
                            </ProgressBar>

                            <Grid Margin="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <fonts:Icon VerticalAlignment="Center" FontSize="16">
                                    <fonts:Icon.Style>
                                        <Style BasedOn="{StaticResource {x:Type fonts:Icon}}" TargetType="{x:Type fonts:Icon}">
                                            <Setter Property="Foreground" Value="{DynamicResource {x:Static joufflu:Brushes.InfoBrush}}" />
                                            <Setter Property="Text" Value="{x:Static fonts:IconFont.Info}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Options.Type, RelativeSource={RelativeSource TemplatedParent}}" Value="Warning">
                                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static joufflu:Brushes.WarningBrush}}" />
                                                    <Setter Property="Text" Value="{x:Static fonts:IconFont.Warning}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Options.Type, RelativeSource={RelativeSource TemplatedParent}}" Value="Error">
                                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static joufflu:Brushes.DangerBrush}}" />
                                                    <Setter Property="Text" Value="{x:Static fonts:IconFont.Warning}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Options.Type, RelativeSource={RelativeSource TemplatedParent}}" Value="Success">
                                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static joufflu:Brushes.SuccessBrush}}" />
                                                    <Setter Property="Text" Value="{x:Static fonts:IconFont.CheckCircle}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </fonts:Icon.Style>
                                </fonts:Icon>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="4,0,0,0"
                                    VerticalAlignment="Center"
                                    Text="{Binding Options.Message, RelativeSource={RelativeSource TemplatedParent}}"
                                    TextWrapping="Wrap" />
                            </Grid>

                            <Button
                                Grid.Column="1"
                                Width="23"
                                Height="23"
                                Margin="0,4,0,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Background="Transparent"
                                BorderThickness="0"
                                Command="{Binding CloseCommand, RelativeSource={RelativeSource TemplatedParent}}">
                                <fonts:Icon FontSize="10" Text="{x:Static fonts:IconFont.X}" />
                            </Button>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
